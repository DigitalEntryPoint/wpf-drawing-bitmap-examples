<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


    <!--  import colors dictionary  -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Colors.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="realBg" TargetType="Grid">
        <Setter Property="Background" Value="{DynamicResource Gray-500}" />
    </Style>


    <Style x:Key="foundryKatana" TargetType="Button">
        <Setter Property="Padding" Value="4" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontFamily" Value="Calibri Light" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="{DynamicResource Gray-500}" />
        <Setter Property="Foreground" Value="{DynamicResource Gray-200}" />
    </Style>

    <Style TargetType="ToolBar">
        <Setter Property="Background" Value="{DynamicResource Gray-500}" />
        <Setter Property="Foreground" Value="{DynamicResource Gray-200}" />
    </Style>

</ResourceDictionary>